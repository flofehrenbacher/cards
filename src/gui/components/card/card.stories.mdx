import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Card } from './card'
import { SchelleIcon } from './deck/bavarian/schelle'
import { HerzIcon } from './deck/bavarian/herz'
import { BlattIcon } from './deck/bavarian/blatt'
import { EichelIcon } from './deck/bavarian/eichel'

<Meta title="Karten/Bayerisches Deck" component={Card} />

# Karten

Das bayrische Deck besteht aus den Farben Eichel, Blatt, Herz sowie Schelle.

<Preview>
  <Story name="Alle Farben auf einen Blick">
    <div style={{ display: 'flex', flexWrap: 'wrap' }}>
      <Card icon={EichelIcon} name="A" style={{ margin: 10 }} />
      <Card icon={BlattIcon} name="7" style={{ margin: 10 }} />
      <Card icon={HerzIcon} name="K" style={{ margin: 10 }} />
      <Card icon={SchelleIcon} name="O" style={{ margin: 10 }} />
    </div>
  </Story>
</Preview>
